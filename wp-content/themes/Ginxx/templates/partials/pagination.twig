{% if posts.pagination.pages is not empty %}
    <div class="ToolPagination-wrapper">
        <div class="ToolPagination-inner">
            {% if posts.pagination.prev %}
                <a href="{{ posts.pagination.prev.link }}"
                   class="prev {{ posts.pagination.prev.link|length ? '' : 'invisible' }}">{{ __('<', 'gosselink') }}</a>
            {% endif %}

            {% for page in posts.pagination.pages %}
                <div>
                    {% if page.link %}
                        <a href="{{ page.link }}" class="{{ page.class }}">{{ page.title }}</a>
                    {% else %}
                        <span class="{{ page.class }}">{{ page.title }}</span>
                    {% endif %}
                </div>
            {% endfor %}

            {% if posts.pagination.next %}
                <a href="{{ posts.pagination.next.link }}"
                   class="next {{ posts.pagination.next.link|length ? '' : 'invisible' }}">{{ __('>', 'gosselink') }}</a>
            {% endif %}
        </div>
    </div>
{% endif %}
