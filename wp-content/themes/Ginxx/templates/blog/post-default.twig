<div class="post-type-{{ post.post_type }}" id="post-{{ post.ID }}">
    <header class="entry-header">
        {# {% include 'blog/meta.twig' %} #}
        {#        {% if post.thumbnail %} #}
        {#            <div class="content-img"> #}
        {#                <img src="{{ TimberImage(post.thumbnail.src) }}" alt="{{ post.title }}"> #}
        {#            </div> #}
        {#        {% endif %} #}
    </header>
    <div class="entry-summary">
        {{ post.content }}
    </div>
</div>