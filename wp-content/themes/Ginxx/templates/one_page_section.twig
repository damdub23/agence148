<section id="{{ section.id }}" class="main-section {{ post.post_name }} {{ section.class }}">

    <div class="page-content">
        {% if section.get_is_popup() %}        
            <div class="close-button">Close</div>
        {% endif %}

        {% block content %}

        {% endblock %}

    </div>

</section>